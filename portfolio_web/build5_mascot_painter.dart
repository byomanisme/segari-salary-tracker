"import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass PixelMascotWidget extends StatelessWidget {\n  final String skinId;\n  final int frameIndex;\n  final Point<int> direction;\n  final double size;\n\n  const PixelMascotWidget({\n    Key? key,\n    required this.skinId,\n    this.frameIndex = 0,\n    this.direction = const Point(1, 0),\n    this.size = 24.0,\n  }) : super(key: key);\n\n  @override\n  Widget build(BuildContext context) {\n    return SizedBox(\n      width: size,\n      height: size,\n      child: CustomPaint(\n        painter: PixelSpritePainter16(\n          skinId: skinId,\n          frameIndex: frameIndex,\n          direction: direction,\n        ),\n      ),\n    );\n  }\n}\n\nclass PixelSpritePainter16 extends CustomPainter {\n  final String skinId;\n  final int frameIndex;\n  final Point<int> direction;\n\n  PixelSpritePainter16({\n    required this.skinId,\n    required this.frameIndex,\n    required this.direction,\n  });\n\n  static const _size = 16;\n\n  // --- PALETTE CONSTANTS ---\n  static const Color _bk = Color(0xFF0F172A); // Dark outline\n  static const Color _wt = Color(0xFFFFFFFF); // White highlight\n  static const Color _cr = Color(0xFFEF4444); // Red\n  static const Color _gd = Color(0xFFFBBF24); // Gold\n\n  // 🐱 Cat Palette\n  static const Color _co = Color(0xFFF59E0B); // Cat Orange Main\n  static const Color _cl = Color(0xFFFCD34D); // Cat Light\n  static const Color _cd = Color(0xFFB45309); // Cat Dark Orange / Stripes\n  static const Color _cp = Color(0xFFFDA4AF); // Pink inner ear / nose\n  static const Color _cg = Color(0xFF10B981); // Emerald Green Eye\n\n  // 🐟 Catfish Palette\n  static const Color _fc = Color(0xFF06B6D4); // Cyan Main\n  static const Color _fl = Color(0xFF67E8F9); // Light Cyan\n  static const Color _fd = Color(0xFF0E7490); // Deep Cyan Shade\n  static const Color _ft = Color(0xFFA5F3FC); // Whisker Glow\n\n  // 🦆 Duck Palette\n  static const Color _dy = Color(0xFFEAB308); // Duck Yellow\n  static const Color _dl = Colo
<truncated 41836 bytes>