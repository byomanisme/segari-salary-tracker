"import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass PixelMascotWidget extends StatelessWidget {\n  final String skinId;\n  final int frameIndex;\n  final Point<int> direction;\n  final double size;\n\n  const PixelMascotWidget({\n    Key? key,\n    required this.skinId,\n    this.frameIndex = 0,\n    this.direction = const Point(1, 0),\n    this.size = 22.0,\n  }) : super(key: key);\n\n  @override\n  Widget build(BuildContext context) {\n    return SizedBox(\n      width: size,\n      height: size,\n      child: CustomPaint(\n        painter: PixelSpritePainter(\n          skinId: skinId,\n          frameIndex: frameIndex,\n          direction: direction,\n        ),\n      ),\n    );\n  }\n}\n\nclass PixelSpritePainter extends CustomPainter {\n  final String skinId;\n  final int frameIndex;\n  final Point<int> direction;\n\n  PixelSpritePainter({\n    required this.skinId,\n    required this.frameIndex,\n    required this.direction,\n  });\n\n  static const _size = 12;\n\n  // --- COLOR PALETTES ---\n  static const Color _co = Color(0xFFF59E0B); // Cat Orange\n  static const Color _cd = Color(0xFFD97706); // Cat Dark Orange\n  static const Color _cw = Color(0xFFFFFFFF); // White\n  static const Color _cp = Color(0xFFFDA4AF); // Pink\n  static const Color _cb = Color(0xFF0F172A); // Black\n  static const Color _cg = Color(0xFF10B981); // Green eye\n  static const Color _cr = Color(0xFFEF4444); // Red collar\n\n  static const Color _fc = Color(0xFF06B6D4); // Fish Cyan\n  static const Color _fd = Color(0xFF0891B2); // Fish Dark Cyan\n  static const Color _fl = Color(0xFF67E8F9); // Fish Light Cyan\n\n  static const Color _dy = Color(0xFFEAB308); // Duck Yellow\n  static const Color _dd = Color(0xFFCA8A04); // Duck Dark Yellow\n  static const Color _do = Color(0xFFF97316); // Duck Orange\n  static const Color _dg = Color(0xFF10B981); // Duck Green Tuft\n\n  static const Color _rp = Color(0xFFF472B6); // Rabbit Pink\n  static const Color _rd = Color(0xFFDB2777); // Rabbit Dark Pink\n  static const
<truncated 25836 bytes>